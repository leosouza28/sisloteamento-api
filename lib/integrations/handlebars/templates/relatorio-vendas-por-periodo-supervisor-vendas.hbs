<!DOCTYPE html>
<html lang="pt-br">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Relatório</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">

    <style>
        page[size="A4"] {
            background: white;
            width: 20cm;
            display: block;
            margin: 0 auto;
        }
    </style>
</head>

<body>
    <div>
        <div class="row mb-3 g-3 text-uppercase">
            <div class="col-12 mb-3">
                <h5 class="text-center fw-bold my-3">Relatório de vendas por período (supervisor de vendas)</h5>
                <p class="mb-0"><strong>Período:</strong> {{periodo}}</p>
                <p class="mb-0"><strong>Emitido por:</strong> {{emitido_por.nome}} em {{emitido_em}}</p>
            </div>
            <div class="col-12">
                <table class="table table-striped">
                    <thead class="table-dark">
                        <tr>
                            <th>Data</th>
                            <th>Plano</th>
                            <th>Categoria</th>
                            <th>Cliente</th>
                            <th>Status</th>
                            <th>Vendedor</th>
                            <th class="text-end">Valor&nbsp;comissão</th>
                            <th class="text-end">Valor&nbsp;Liquido</th>
                        </tr>
                    </thead>
                    <tbody>
                        {{#each lista_vendas}}
                        <tr>
                            <td>{{data_venda_brl}}</td>
                            <td>{{plano.codigo}}&nbsp;{{plano.nome}}</td>
                            <td>{{plano.categoria.descricao}}</td>
                            <td>{{cliente.nome}}</td>
                            <td>{{status}}</td>
                            <td>{{vendedor.nome}}&nbsp;{{vendedor.documento}}</td>
                            <td class="text-end">{{valor_comissao_brl}}</td>
                            <td class="text-end">{{valor_liquido_brl}}</td>
                        </tr>
                        {{/each}}
                    </tbody>
                    <tfoot>
                        <tr>
                            <th colspan="6">Total {{geral.qtd_total_planos_vendidos}} planos</th>
                            <th class="text-end">{{geral.valor_total_comissao_brl}}</th>
                            <th class="text-end">{{geral.valor_total_liquido_brl}}</th>
                        </tr>
                    </tfoot>
                </table>
            </div>
            <div class="col-12">
                <h5 class="text-center fw-bold">Resumo por vendedor</h5>
            </div>
            <div class="col-12">
                <table class="table table-striped">
                    <thead class="table-dark">
                        <tr>
                            <th>Vendedor</th>
                            <th class="text-center">Qtd</th>
                            <th class="text-end">Valor&nbsp;Total</th>
                        </tr>
                    </thead>
                    <tbody>
                        {{#each vendas_agrupadas_por_vendedor}}
                        <tr>
                            <td>{{nome}}</td>
                            <td class="text-center">{{qtd_vendas}}</td>
                            <td class="text-end">{{valor_liquido}}</td>
                        </tr>
                        {{/each}}
                    </tbody>
                </table>
            </div>
            <div class="col-12">
                <h5 class="text-center fw-bold">Resumo por plano</h5>
            </div>
            <div class="col-12">
                <table class="table table-striped">
                    <thead class="table-dark">
                        <tr>
                            <th>Plano</th>
                            <th>Categoria</th>
                            <th class="text-center">Qtd</th>
                            <th class="text-end">Valor&nbsp;Total</th>
                        </tr>
                    </thead>
                    <tbody>
                        {{#each vendas_por_plano_categoria}}
                        <tr>
                            <td>{{nome}}</td>
                            <td>{{categoria}}</td>
                            <td class="text-center">{{qtd_vendas}}</td>
                            <td class="text-end">{{valor_liquido}}</td>
                        </tr>
                        {{/each}}
                    </tbody>
                </table>
            </div>
            <div class="col-12">
                <h5 class="text-center fw-bold">Resumo por formas de pagamento</h5>
            </div>
            <div class="col-12">
                <table class="table table-striped">
                    <thead class="table-dark">
                        <tr>
                            <th>Forma&nbsp;Pagamento</th>
                            <th class="text-end">Valor&nbsp;Total</th>
                        </tr>
                    </thead>
                    <tbody>
                        {{#each vendas_formas_pagamento}}
                        <tr>
                            <td>{{forma_pagamento}}</td>
                            <td class="text-end">{{valor}}</td>
                        </tr>
                        {{/each}}
                    </tbody>
                </table>
            </div>
        </div>
        <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>

</html>