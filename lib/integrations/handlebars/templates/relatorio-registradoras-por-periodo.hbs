<!DOCTYPE html>
<html lang="pt-br">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Relatório</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">

    <style>
        page[size="A4"] {
            background: white;
            width: 20cm;
            display: block;
            margin: 0 auto;
        }
    </style>
</head>

<body>
    <div>
        <div class="row mb-3 g-3 text-uppercase">
            <div class="col-12 mb-3">
                <h5 class="text-center fw-bold my-3">Relatório de registradoras por período</h5>
                <p class="mb-0"><strong>Período:</strong> {{periodo}}</p>
                <p class="mb-0"><strong>Filtro da data:</strong> {{tipo_fechamento}}</p>
                <p class="mb-0"><strong>Emitido por:</strong> {{emitido_por.nome}} em {{emitido_em}}</p>
            </div>
            <div class="col-12">
                <table class="table table-striped">
                    <thead class="table-dark">
                        <tr>
                            <th>Abertura</th>
                            <th>Fechamento</th>
                            <th>Operador</th>
                            <th>Prestou&nbsp;conta</th>
                            <th>Prestador&nbsp;conta</th>
                            <th class="text-end">Valor&nbsp;Total</th>
                        </tr>
                    </thead>
                    <tbody>
                        {{#each lista_registradoras}}
                        <tr>
                            <td>{{data_abertura_brl}}</td>
                            <td>{{data_fechamento_brl}}</td>
                            <td>{{usuario.nome}}</td>
                            <td>{{_prestou_conta}}</td>
                            <td>{{prestou_conta_por.usuario.nome}}</td>
                            <td class="text-end">{{total_registradora_brl}}</td>
                        </tr>
                        {{/each}}
                    </tbody>
                    <tfoot>
                        <tr>
                            <td colspan="5">Total</td>
                            <td class="text-end">{{valor_total_registradoras_brl}}</td>
                        </tr>
                    </tfoot>
                </table>
            </div>
            <div class="col-12">
                <h5 class="text-center fw-bold">Agrupado por Forma de Pagamento</h5>
            </div>
            <div class="col-12">
                <table class="table table-striped">
                    <thead class="table-dark">
                        <tr>
                            <th>Forma&nbsp;pagamento</th>
                            <th class="text-end">Total</th>
                        </tr>
                    </thead>
                    <tbody>
                        {{#each lista_agrupada}}
                        <tr>
                            <td>{{descricao}}</td>
                            <td class="text-end">{{valor_total_brl}}</td>
                        </tr>
                        {{/each}}
                    </tbody>
                    <tfoot>
                        <tr>
                            <th>Total</th>
                            <th class="text-end">{{total_agrupado_brl}}</th>
                        </tr>
                    </tfoot>
                </table>
            </div>
        </div>
        <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
    </div>
</body>

</html>